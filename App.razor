<CascadingAuthenticationState>
<Router AppAssembly="@typeof(App).Assembly">
    <Found Context="routeData">
        <AuthorizeRouteView RouteData="@routeData">
            <NotAuthorized>
                <RedirectToLogin IsAuthenticated="@context.User.Identity.IsAuthenticated" />
            </NotAuthorized>
            <Authorizing></Authorizing>
        </AuthorizeRouteView>
    </Found>
    <NotFound>
        <PageTitle>Ikke fundet</PageTitle>
        <LayoutView Layout="@typeof(MainLayout)">
            <RadzenRow>
                <RadzenColumn Size="12" style="margin-top: 5rem; margin-bottom: 5rem">
                    <RadzenText Text="Siden blev ikke fundet" TextStyle="TextStyle.DisplayH1" style="margin: 0; margin-bottom: 2rem" TextAlign="TextAlign.Center" />
                    <RadzenText Text="Beklager, men der er intet her!" TextStyle="TextStyle.H6" style="margin: 0" TextAlign="TextAlign.Center" TagName="TagName.P" />
                </RadzenColumn>
            </RadzenRow>
        </LayoutView>
    </NotFound>
</Router>
</CascadingAuthenticationState>
